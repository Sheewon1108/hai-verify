/**
 * Compatibility shim — canonical: `@/hai-ic/core` (`HAI_IC_DD_MAX_PENALTY_LIVE`).
 * Daemon scripts may still patch this path; keep export name stable.
 */
export { HAI_IC_DD_MAX_PENALTY_LIVE } from "@/hai-ic/core";
