/**
 * Compatibility shim — canonical: `@/hai-ic/core` (`HAI_IC_HOURLY_BOOST`).
 * Daemon scripts may still patch this path; keep export name stable.
 */
export { HAI_IC_HOURLY_BOOST } from "@/hai-ic/core";
