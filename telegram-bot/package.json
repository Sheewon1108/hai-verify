{
  "name": "hai-ic-verify-bot",
  "version": "0.1.0",
  "private": true,
  "description": "Hai_ic_verify_bot — Telegram verify intake (fetch only, no telegraf)",
  "scripts": {
    "start": "node index.js",
    "test": "node --test index.test.js"
  },
  "engines": {
    "node": ">=18"
  }
}
