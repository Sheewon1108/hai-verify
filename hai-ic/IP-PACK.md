# HAI-IC — IP Pack (1 page)

**Owner:** KARAM SHIN · XGOMA, Inc.  
**Product name:** HAI-IC only  
**Scarcity:** Philosophy-level original IP → live paid system → solo operator → cash rail live

---

## One-line thesis

**Before AI acts, humans measure Intent Confidence (0–100); Sincere Mode only at 75%+; the human keeps the final decision and the responsibility.**

---

## Problem → mechanism → proof

| Stage | Fact |
|-------|------|
| **Problem** | LLMs and agents answer under uncertainty → hallucination, fake ETA, DD exaggeration, irreversible tool actions. |
| **Mechanism** | Pre-LLM **Intent Confidence Gate**: score 0–100 → ≥75% Sincere Mode ON (answer) / &lt;75% OFF (clarify / evidence first). Human approval remains mandatory before execution. |
| **Proof (live)** | Production analyze + health endpoints on Cloudflare Workers; local owner loopback `127.0.0.1:3001`; Trust Ledger from live test runs (not hand-inflated scores). |
| **Proof (paid rail)** | Stripe Payment Link CTA live on `/hai-ic` (`Start Paid Pilot`). Webhook path issues API keys on `checkout.session.completed` when secrets are set. |

**Trust Ledger snapshot (buyer-deliverables, 2026-07-08):** 200 questions · ON 82 (41%) · OFF 118 (59%) · DD blocked 115/126 · avg IC 75.8% · `HAI_IC_HOURLY_BOOST = 0`.

---

## Why hard to copy (process + philosophy — not only model/code)

1. **Philosophy lock** — Score inflation banned; OFF is a product feature (risk blocked), not a failure mode.
2. **Responsibility lock** — Gate never takes the final decision; human retains approval and liability.
3. **Process lock** — Outreach sincerity validation, Trust Ledger / OFF-case exports, backup restore-point discipline, vault-only secrets.
4. **Operating lock** — Solo founder IP + live deploy + payment CTA + audit trail as one system; cloning UI or regex does not clone the operating doctrine.

---

## Evidence (no exaggeration)

| Claim | Status |
|-------|--------|
| Live HAI-IC API (`/api/hai-ic/analyze`, `/api/hai-ic/health`) | **Yes** — deployed surface `hai-verify.workers.dev` |
| Live paid pilot CTA (Stripe Payment Link) | **Yes** — public link on landing |
| Trust Ledger from real analyzer runs | **Yes** — 200-question sample in repo |
| Real cash collected (settled $ in bank / Stripe balance) | **Not recorded in this repo** — do not claim until KARAM logs settlement ID |

**Commercial ask:** Pilot → Team $8.5k/yr · Enterprise $25k/yr · Contact: jay.transtar.inc@gmail.com

---

*Patents (founder context): USPTO #19/546,296 · #19/544,919 · #63/985,005 — cited as KARAM ownership context; this pack does not assert claim coverage.*
