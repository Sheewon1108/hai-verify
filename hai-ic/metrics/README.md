# HAI-IC metrics output

Run:

```bash
npm run hai-ic:metrics
# or
BASE_URL=https://hai-verify.workers.dev N=100 npm run hai-ic:metrics
```

Writes `latest.json` (processing speed, latency p50/p95).  
Copy numbers into `../METRICS-PLAN.md`. Do not invent values.
