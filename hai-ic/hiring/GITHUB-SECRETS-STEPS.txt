GitHub Actions Secrets — war room step (browser only, ~3 min)

1. Open (login + passkey if asked):
   https://github.com/Sheewon1108/hai-verify/settings/secrets/actions

2. New repository secret — add TWO (names exact):

   CLOUDFLARE_API_TOKEN
   = paste token from Cloudflare dashboard (Edit Cloudflare Workers template)

   CLOUDFLARE_ACCOUNT_ID
   = paste Account ID from Cloudflare dashboard home (right sidebar)

3. Deploy once:
   https://github.com/Sheewon1108/hai-verify/actions/workflows/deploy-cloudflare.yml
   → Run workflow → branch main → Run

4. Tell agent in war room: "secrets 했어"
   Agent checks Actions log + production /api/verify 401 (no key).

Do NOT put tokens in Cursor chat.
Values live in: GitHub Secrets OR local vault — not both required for first deploy.